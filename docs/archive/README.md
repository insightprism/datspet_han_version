# docs/archive — retired specs and reference material

Documents here are **historical**: superseded, never built, or one-shot plans that were
executed to completion. Each archived spec carries an `ARCHIVED <date>` banner at the top
naming the reason and the living successor. Do not implement from these documents; the
authoritative specs live one level up in `docs/`.

| Document | Why archived | Living successor |
|---|---|---|
| `DATSME_INTEGRATION.md` | *(2026-07-16)* Never built — plan to embed pet generation inside the DatsMe host; the shipped architecture inverted it (DatsPet as standalone DPP partner). | `docs/SPEC_DATSPET_HOUSE_ADOPT.md`; build records in `docs/archive/SPEC_DATSPET_DPP_INTEGRATION.md` + `SPEC_DATSPET_FRONT_DOOR.md` |
| `DESIGN_SPEC_APPLICATION.md` | Point-in-time snapshot of the v1.0.0 `pet_factory` library; the code evolved past it (registries, `pose_frames`, design axes). | `docs/SPEC_MOTION_PROFILES.md`, `docs/SPEC_PET_DESIGNER_FLOW.md`, `docs/SPEC_PET_DESIGN_AXES.md`, `CLAUDE.md` |
| `DESIGN_SPEC_COMPUTE_POOL.md` | Origin design study for the compute pool, since built as the sibling `../shared_gpu_cpu` project whose own docs are the authority. | `../shared_gpu_cpu/docs/`, `webui/pool_client.py`, `docs/SPEC_DEPLOY_PETDATSME_POOL.md` |
| `DESIGN_SPEC_HETZNER_LOCAL_GPU.md` | Superseded bespoke-queue deployment — `SPEC_DEPLOY_PETDATSME_POOL.md` §2 says "do not build". | `docs/SPEC_DEPLOY_PETDATSME_POOL.md`, `deploy/CHECKLIST.md` |
| `SPEC_V3_FLEET_ROLLOUT.md` | One-shot v3 handler fleet cutover, executed to completion 2026-07-13. | `deploy/CHECKLIST.md` item A6, `docs/SPEC_DEPLOY_PETDATSME_POOL.md` §B.1 |
| `SPEC_GPU_MEMORY_HYGIENE.md` | Five GPU-memory/observability fixes (F1–F5), executed to completion 2026-07-26 and verified against real builds. **Exception to "do not read": its §10 is live reference — the MEASURED dead ends for generation speed (`--highvram` OOMs, resolution is not a lever, single-expert fringes, `_fill_holes_alpha` is noise) plus the ComfyUI graph+seed caching trap. Read §10 before any generation-speed work.** | the shipped code: `pet_factory/factory.py` constants band + `_CutoutSession`, `webui/app.py` logging, `pool_handler/*.py`; guarded by `pet_factory/tests/test_cutout_hygiene.py` and `webui/tests/test_logging_visibility.py`; re-runnable evidence in `scripts/probe_cutout_arena.py` |
| `SPEC_DATSPET_FEDERATED_SESSION.md` | *(2026-07-30)* Executed in full and verified on staging: sign-in/sign-out/stay-signed-in, the per-browser anonymous owner, and the consolidation of two purchase paths onto one. **Exception to "do not read": it is still the authority for the owner-scope model (§4.5), the renewal rule (§4.2) and *why* the push path is gone (§6)** — specs and code comments cite those sections by number. | the shipped code: `webui/datsme_integration.py`, `webui/owner_scope.py`, `web/src/lib/api.ts`; `docs/SPEC_DATSPET_HOUSE_ADOPT.md` (the pull it consolidated onto), `docs/archive/SPEC_DATSPET_CATALOG_PURCHASE.md` (split out) |
| `SPEC_DATSPET_DPP_INTEGRATION.md` | *(2026-07-30)* The founding DatsPet-as-DPP-partner spec, built and E2E-verified 2026-07-11 and corrected against both working trees at closeout. Half of it still runs (manifest, `/launch` + cookie, export, revoke, bundle tokens, the SQLite store, the host's `user.pet` target); the other half — Accept, the push writeback, the retry queue, `rsx` — was retired by the federated-session consolidation. Kept as the build record and the only place the `pet_bundle.v1` payload and the registration bootstrap are written down. | `docs/SPEC_DATSPET_HOUSE_ADOPT.md`, `docs/archive/SPEC_DATSPET_CATALOG_PURCHASE.md`, `docs/RUNBOOK_DPP_E2E.md`; `docs/archive/SPEC_DATSPET_FEDERATED_SESSION.md` + `SPEC_DATSPET_FRONT_DOOR.md` |
| `SPEC_DATSPET_FRONT_DOOR.md` | *(2026-07-30)* Public landing + "Sign in with DatsMe" — built in full, deployed to prod **and** staging, and re-probed live at closeout (both hosts' `login-launch` 302s to `/login?next=…`; both partners report `integrated` with their own `signin_url`). **Exception to "do not read": §2.1 is still the definition of the shared `resolve_and_mint_launch` helper and `extra_claims`**, which `login-launch`, `admin-launch` and `POST /launch` are all thin wrappers over — `SPEC_MOTION_PROFILE_ADMIN` cites it by section. Its §3.3 logout was superseded by federated §4.1; its §9.2/§9.3 remain open product decisions. | the shipped code: `web/src/components/PublicLanding.tsx`, `webui/datsme_integration.py` (`_safe_return_path`, session), `datsme_me/api/apps/dpp/{routes,service}.py`, `datsme_me/web/src/app/integrations/consent/page.tsx`; guarded by `webui/tests/test_front_door.py` + `datsme_me/api/tests/test_front_door_launch.py`; `docs/SPEC_MOTION_PROFILE_ADMIN.md` (the admin sibling) |
| `*.html` + `*_files/` (untracked) | Browser-saved research pages whose conclusions were absorbed into specs: the two motion studies fed `SPEC_MOTION_PROFILES.md` §3.9; the Phase 3 contact sheet's durable record is `pet_factory/design_axes/calibration/`. | the specs named per row |
